# Lab 6
- Distinguish the difference between regression and classification models.
- I learned the difference between regression and classification models. I also learned about the different types of regression and classification models, with one of the regression models actually being used in my midterm project (logistic regression).
